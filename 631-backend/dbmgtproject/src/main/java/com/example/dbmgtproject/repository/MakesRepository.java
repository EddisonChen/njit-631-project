package com.example.dbmgtproject.repository;

import com.example.dbmgtproject.model.Makes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MakesRepository extends JpaRepository<Makes, Integer> {
}
