package com.example.dbmgtproject.repository;

import com.example.dbmgtproject.model.ReactsWith;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReactsWithRepository extends JpaRepository<ReactsWith, Integer> {
}
