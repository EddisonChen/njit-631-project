package com.example.dbmgtproject.repository;

import com.example.dbmgtproject.model.Requires;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequiresRepository extends JpaRepository<Requires, Integer> {
}
