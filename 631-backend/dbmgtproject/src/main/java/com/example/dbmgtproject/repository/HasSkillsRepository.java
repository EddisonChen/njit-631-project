package com.example.dbmgtproject.repository;

import com.example.dbmgtproject.model.HasSkills;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HasSkillsRepository extends JpaRepository<HasSkills, Integer> {
}
